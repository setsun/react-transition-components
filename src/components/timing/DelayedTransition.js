import React from 'react';
import { Transition } from 'react-transition-group';

type DelayedTransitionProps = {
  delay: number,
};

class DelayedTransition extends React.Component<DelayedTransitionProps> {
  render() {
    return <div />;
  }
}
